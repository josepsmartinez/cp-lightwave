#ifndef MATH_EXT_H


using namespace std;
typedef double unit;
typedef unsigned char byte;

#define ABS(a) ((a)<0 ? (-(a)) : (a))
#define min(X,Y) ((X) < (Y) ? (X) : (Y))
#define max(X,Y) ((X) > (Y) ? (X) : (Y))







#define MATH_EXT_H

#endif // MATH_EXT_H
